# راهنمای نصب و تست — v2.15 (رفع دسترسی آرایشگر)

## نصب (روی دیتابیس موجود v2.14)
۱. فایل `supabase/migrations/v2.15_stylist_access_fix.sql` را در SQL Editor پروژهٔ Supabase اجرا کنید (مستقل، idempotent، بدون حذف داده).
۲. کد فرانت‌اند جدید (`src/App.jsx`) را دیپلوی کنید.
۳. برای نصب تازه: از همین `schema.sql` (که این تغییرات را هم دارد) استفاده کنید — نیازی به اجرای جداگانهٔ migration نیست.

## ترتیب تست پیشنهادی
۱. **کاربر عمومی**: صفحهٔ رزرو را باز کنید، یک نوبت بسازید — باید مثل قبل کار کند.
۲. **آرایشگر ۱**: با شماره‌اش وارد شوید → فقط «نوبت‌های من» و «ساعات کاری من» را ببینید؛ تب‌های حسابداری/خدمات/آرایشگرها/پیامک/تحلیل هوشمند/هوش تجاری نباید نمایش داده شوند.
۳. **آرایشگر ۲**: همین تست را با شماره‌ی دیگر تکرار کنید — نباید نوبت‌های آرایشگر ۱ را ببیند.
۴. **مدیر/مالک**: وارد شوید → همهٔ تب‌ها و امکانات قبلی باید دست‌نخورده باشند.
۵. (اختیاری، برای تایید عمقی) با ابزار Supabase (SQL Editor یا REST) به‌عنوان یک کاربر آرایشگر مستقیماً `select * from appointments` بزنید — باید فقط ردیف‌های خودش برگردد.

## خلاصهٔ فایل‌های تغییرکرده
- `supabase/schema.sql` — همان اصلاحات، برای نصب تازه
- `supabase/migrations/v2.15_stylist_access_fix.sql` — فایل مستقل ارتقا (جدید)
- `src/App.jsx` — مخفی‌کردن تب‌های حسابداری/خدمات/تحلیل هوشمند برای آرایشگر (هم در منو هم در رندر محتوا)، بستن کارت‌های RFM/ماتریس دسته‌بندی، اصلاح خروج از حساب

## نتیجهٔ واقعی build و آزمون‌ها (در این نشست اجرا شد)
- `npm run build`: موفق، بدون خطا
- ESLint: بدون خطای واقعی
- تعادل پرانتز SQL (بعد از حذف کامنت‌ها): هر دو فایل متعادل
- ۱۶ تست واحد روی منطق RLS (owner/manager/دو آرایشگر مجزا/حساب غیرفعال/نبود stylist_id) — همه موفق
- ۱۰ تست تعاملی رزرو عمومی + دسترسی مدیر — همه موفق
- ۱۰ تست تعاملی آرایشگر ۱ — همه موفق
- ۱۰ تست تعاملی آرایشگر ۲ — همه موفق
- بررسی `diff` با نسخهٔ ۲.۱۴: فقط `App.jsx`، `schema.sql`، و فایل migration جدید تغییر کرده‌اند — هیچ فایل نامرتبطی دست نخورده

## یک یافتهٔ خارج از محدودهٔ این تسک (نیاز به تصمیم شما)
تابع `bootstrap()` در `api.js` کل جدول `appointments` را حتی برای بازدیدکنندهٔ ناشناس (صفحهٔ رزرو عمومی) می‌خواند، و سیاست RLS برای کاربر ناشناس در این نسخه دست‌نخورده ماند (`auth.uid() is null` همچنان مجاز است) — چون رفعش نیاز به تغییر معماری واکشی داده برای فلوی رزرو عمومی دارد (جدا کردن یک نمای عمومی بدون اطلاعات مشتری) و فراتر از «حداقل تغییرات برای مشکل نقش stylist» است. اگر می‌خواهید این را هم در یک migration جداگانه رفع کنم، اطلاع دهید.
