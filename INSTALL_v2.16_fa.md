# راهنمای نصب و تست — v2.16 (رفع نشتی اطلاعات مشتریان برای بازدیدکنندهٔ ناشناس)

## نصب (روی دیتابیس موجود v2.15)
۱. `supabase/migrations/v2.16_public_slots_privacy.sql` را در SQL Editor اجرا کنید (مستقل، idempotent، بدون حذف داده).
۲. کد فرانت‌اند جدید را دیپلوی کنید.
۳. نصب تازه: از همین `schema.sql` استفاده کنید.

## چی رفع شد
`bootstrap()` قبلاً کل جدول `appointments` (نام/شماره/قیمت هر مشتری) را حتی برای بازدیدکنندهٔ ناشناسِ صفحهٔ رزرو می‌خواند. حالا:
- بازدیدکنندهٔ ناشناس فقط از یک View بدون اطلاعات خصوصی (`appointments_public_slots`) می‌خواند — فقط داده‌ی لازم برای محاسبهٔ زمان‌های خالی.
- مشتری برای دیدن نوبت‌های خودش («داشبورد من») شماره‌اش را تایپ می‌کند و از یک RPC جدید (`get_my_bookings`) فقط نوبت‌های همان شماره را می‌گیرد.
- کارمندان (مدیر/آرایشگر) بعد از ورود، دادهٔ کامل را جداگانه دریافت می‌کنند — طبق محدودیت‌های نقشی v2.15، بدون تغییر.

## محدودیت شناخته‌شده (عمداً در این نسخه رفع نشد)
به‌روزرسانی زندهٔ تقویم (Realtime) برای بازدیدکنندهٔ ناشناس دیگر کار نمی‌کند (بی‌صدا، بدون خطا) چون Realtime روی View به‌طور قابل‌اعتماد کار نمی‌کند. کاربر باید تاریخ را دوباره انتخاب کند تا زمان‌های جدید را ببیند. رفع کامل نیاز به یک مکانیزم دیگر دارد؛ اگر بخواهید جدا انجام می‌شود.

## ترتیب تست
۱. **عمومی**: یک نوبت بسازید — باید زمان‌های خالی درست محاسبه و نوبت درست ثبت شود.
۲. **همان مشتری در «داشبورد من»**: با همان شماره جست‌وجو کنید — باید نوبت را ببیند و بتواند لغو/جابه‌جا کند.
۳. **مدیر**: وارد شوید — همهٔ داده‌ها (نام/شماره/قیمت مشتریان) باید مثل قبل کامل دیده شود.
۴. **آرایشگر**: محدودیت‌های v2.15 (تب‌های مخفی) باید دست‌نخورده باشد.
۵. (اختیاری) با کلاینت Supabase بدون لاگین، مستقیم `select * from appointments` بزنید — باید صفر ردیف برگردد؛ `select * from appointments_public_slots` باید فقط ستون‌های غیرخصوصی را بدهد.

## خلاصهٔ فایل‌های تغییرکرده
- `supabase/schema.sql`, `supabase/migrations/v2.16_public_slots_privacy.sql`
- `src/lib/api.js` — `fetchFullAppointments`, `fetchMyBookings`؛ `bootstrap()` اصلاح شد
- `src/App.jsx` — واکشی جداگانهٔ دادهٔ کامل برای کارمند، `TrackView` روی RPC جدید

## نتیجهٔ واقعی build و تست (در این نشست اجرا شد)
- `npm run build`: موفق
- ESLint، تعادل پرانتز SQL: تایید شد
- ۴ تست تعاملی (رزرو کامل، پیگیری مشتری، دسترسی مدیر): موفق
- ۹ تست رگرسیون تب‌های مدیر + ۶ تست رگرسیون آرایشگر (محدودیت‌های v2.15 دست‌نخورده): موفق
- `diff` با v2.15: فقط `App.jsx`، `api.js`، `schema.sql`، و migration جدید تغییر کرده‌اند
