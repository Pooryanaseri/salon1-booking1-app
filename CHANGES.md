# تغییرات نسخهٔ ۲ — بک‌اند واقعی + پیامک واقعی

## دو نکته که با فرض اولیه‌ات فرق داشت

**۱. هیچ `localStorage`ای در کد فعلی وجود نداشت.** کل داده در `useState` با
seed زندگی می‌کرد و با هر رفرش صفر می‌شد (README خودت هم همین را گفته بود).
پس «مایگریشن از localStorage» موضوعیت ندارد — دادهٔ ذخیره‌شده‌ای نبود که از
دست برود. دادهٔ seed را عیناً به `supabase/seed.sql` منتقل کردم تا همان ۱۴ خدمت،
۳ آرایشگر و ساعات کاری از روز اول در دیتابیس باشد.

**۲. Tailwind نصب نبود.** در `package.json` نبود، در `index.html` هم CDN نداشت.
کلاس‌هایی مثل `flex items-center gap-2` در JSX هستند ولی **هیچ استایلی روی آن‌ها
سوار نیست** — چیدمان واقعی از ۴۷۴ تا `style={{...}}` inline و متغیرهای CSS در
`TOKENS_CSS` می‌آید. پس همان سیستم را ادامه دادم: tokens + inline styles + همان
کلاس‌های `card` / `tap` / `accent-btn` / `muted` / `badge`. اگر واقعاً Tailwind
می‌خواهی، آن یک کار جداست و باید صریح انجام شود.

---

## dependencies جدید

فقط **یکی**:

```
@supabase/supabase-js  ^2.45.4
```

بدون تغییر: `react`, `react-dom`, `lucide-react`, `vite`, `@vitejs/plugin-react`.
هیچ کتابخانهٔ تاریخ، state manager، router یا UI kit اضافه نشد.

سمت سرور (Deno، بدون نصب): Edge Functions همان `@supabase/supabase-js` را از
esm.sh می‌گیرند و `fetch` بومی را برای API پنل پیامک استفاده می‌کنند.

---

## فایل‌های جدید

```
src/lib/supabase.js                          کلاینت + تشخیص «متصل نیست»
src/lib/auth.js                              ورود/ثبت‌نام با شماره موبایل
src/lib/api.js                               لایهٔ دسترسی داده + diff-sync
src/lib/sms.js                               کلاینت پیامک + رندر قالب
supabase/schema.sql                          جدول‌ها، RLS، تریگرها، RPCها
supabase/seed.sql                            انتقال دادهٔ فعلی
supabase/cron.sql                            زمان‌بندی یادآوری‌ها
supabase/functions/_shared/providers.ts      کاوه‌نگار + ملی‌پیامک
supabase/functions/send-sms/index.ts         ارسال و صف‌گذاری
supabase/functions/cron-reminders/index.ts   ارسال یادآوری‌های سررسیده
.env.example                                 متغیرهای محیطی
DEPLOY.md                                    راهنمای راه‌اندازی
```

---

## چه چیزی در `App.jsx` عوض شد

**هیچ کامپوننت فرزندی دست‌نخورده نماند** — دقیقاً یعنی: `BookingFlow`،
`TrackView`، `DashboardTab`، `AccountingTab`، `ServicesTab`، `StaffTab`،
`ScheduleTab`، `BITab`، `AIAnalysisTab`، مودال‌ها و atomهای UI **صفر خط تغییر**
داشتند. propهایشان هم عین قبل است.

### ۱. لایهٔ `usePersistedState` — قلب ماجرا

مشکل: `setServices`, `setTimeOff`, `setApprovedDates`, `setWorkingHours`,
`setStaffWorkingHours` مستقیم به فرزندها پاس داده می‌شدند و آن‌ها با فرم
`setX(prev => next)` صدایشان می‌زدند. برای عوض نکردن فرزندها، **قرارداد setter
را حفظ کردم** و نوشتن در دیتابیس را پشت آن گذاشتم:

```js
const [services, setServices] = usePersistedState(SEED_SERVICES, persistServices);
```

هر بار که setter صدا زده می‌شود، آرایهٔ قبلی و جدید بر اساس `id` دیف می‌شوند و
دقیقاً همان insert / update / delete لازم می‌رود. `arm(false)` هنگام بارگذاری
اولیه نوشتن را خاموش می‌کند تا hydrate کردن، خودش تبدیل به سیل UPDATE نشود.

نکتهٔ فنی: `next` بیرون از `setValue` حساب می‌شود (نه داخل updater) تا در
StrictMode که updaterها دو بار اجرا می‌شوند، پیامک و کوئری دوباره نرود.

### ۲. bootstrap و realtime

- یک `Promise.all` همهٔ جدول‌ها را موازی می‌خواند؛ تا رسیدنش دادهٔ seed نمایش
  داده می‌شود، پس first paint هیچ‌وقت خالی نیست.
- `subscribeAppointments` تقویم را زنده نگه می‌دارد: اگر مشتری از موبایل رزرو
  کند، پنل باز روی لپ‌تاپ بدون رفرش آپدیت می‌شود.
- اگر متغیرهای محیطی نباشند یا دیتابیس جواب ندهد، برنامه به حالت دمو برمی‌گردد
  و یک نوار هشدار نشان می‌دهد. صفحهٔ سفید غیرممکن است.

### ۳. احراز هویت

فرم ورود **عین قبل**: شماره موبایل + رمز، همان دو تب «ورود آرایشگر» و «ورود مدیر
سالن». زیرش Supabase Auth نشسته: شماره به ایمیل ساختگی
`09xxxxxxxxx@salon.local` نگاشت می‌شود.

نقش‌ها **عیناً** `owner` / `manager` / `stylist` ماندند، در `public.users.role`،
و حالا **دو لایه** محافظت دارند:

- UI: همان `currentStylist ? [] : [...]` قبلی برای مخفی‌کردن تب‌ها
- دیتابیس: RLS — آرایشگر حتی با درخواست دستی هم نمی‌تواند نوبت همکارش را عوض کند

`session` پایدار است؛ رفرش صفحه دیگر بیرون نمی‌اندازد.

⚠️ **رمز `1234` دیگر کار نمی‌کند** — Supabase حداقل ۶ کاراکتر می‌خواهد. گام ۳
`DEPLOY.md` را ببین.

### ۴. پیامک — یک نقطهٔ تزریق

تمام تغییرات وضعیت نوبت در برنامه از دو تابع می‌گذرد: `addBooking` و
`updateBooking`. پس پیامک را همان‌جا وصل کردم و خودبه‌خود همهٔ مسیرها پوشش
داده شد (فرم مشتری، تب پیگیری، منوی پنل):

| رویداد | اتفاق |
|---|---|
| رزرو جدید | پیامک تایید + صف‌گذاری یادآوری |
| تایید توسط پرسنل | پیامک تایید + صف‌گذاری یادآوری |
| لغو | یادآوری صف حذف + پیامک لغو |
| تغییر زمان | یادآوری قدیمی حذف + پیامک زمان جدید + یادآوری جدید |
| عدم حضور | یادآوری صف حذف |

زمان یادآوری از `reminder_hours_before` **همان آرایشگر** می‌آید — همان فیلدی که
از قبل روی رکورد آرایشگر داشتی.

کلید API هیچ‌وقت به مرورگر نمی‌رسد. مرورگر فقط Edge Function را صدا می‌زند؛
کلید به‌عنوان secret آن‌جا زندگی می‌کند. برای مشتری ناشناس (پیامک تایید نوبت
خودش) فقط سه نوع پیام مجاز است و سقف ۵ پیامک در ساعت برای هر شماره اعمال می‌شود.

### ۵. تب جدید «پیامک» (فقط owner/manager)

`SmsTab` — تنها کامپوننت کاملاً جدید:

- **مخاطبان**: نوبت‌های امروز / فردا / ۷ روز آینده / همهٔ مشتری‌ها / جذب مجدد / شمارهٔ دستی
- **قالب‌های آماده** از `sms_templates` با placeholderهای کلیک‌شدنی
  (`{{name}}`, `{{service}}`, `{{date}}`, `{{time}}`, `{{stylist}}`, `{{code}}`, `{{discount}}`, `{{points}}`)
- **پیش‌نمایش زنده** با دادهٔ گیرندهٔ اول + تعداد تخمینی پیامک (۷۰ کاراکتر = یک پیامک فارسی)
- **ارسال دسته‌جمعی** با گزارش موفق/ناموفق
- **تاریخچهٔ ارسال** با وضعیت و علت خطا

---

## فاز ۳ و ۴ — وضعیت فعلی

**کمپین جذب مجدد (فاز ۳): کار می‌کند.** داخل همان تب پیامک است چون همان
ماشین‌آلات را می‌خواهد:

- اسلایدر «بیش از X روز بدون رزرو» → `inactive_customers(days)` لیست را می‌سازد
- اسلایدر درصد تخفیف → داخل متن با `{{discount}}` می‌نشیند
- ارسال، یک ردیف `campaigns` و کوهورت `campaign_targets` می‌سازد
- تریگر دیتابیس: هر رزرو از یک مخاطب هدف، `returned=true` می‌زند و
  `campaigns.returned_count` را به‌روز می‌کند → **نرخ بازگشت خودکار حساب می‌شود**
- کاری که مانده: نمایش این عدد داخل تب BI (دادهٔ خام آماده است)

**باشگاه مشتریان (فاز ۴): دیتابیسش کامل، UI ندارد.**

- `customers.loyalty_points` + `loyalty_ledger` (دفتر append-only)
- هر نوبتی که `انجام شده` شود، تریگر امتیاز `services.loyalty_points` را واریز
  می‌کند — **همین حالا فعال است**، پس امتیازها از امروز جمع می‌شوند
- `loyalty_settings`: هر ۱۰۰ امتیاز = ۵٪ تخفیف، سقف ۳۰٪، معرفی دوست = ۵۰ امتیاز
- `customer_loyalty(phone)` → امتیاز، درصد تخفیف، کد معرف، تاریخچه
- `apply_referral(phone, code)` → واریز امتیاز به هر دو طرف
- کاری که مانده: پنل مشتری و تخفیف پلکانی خودکار روی قیمت

یعنی تا وقتی UI فاز ۴ را بسازیم، امتیاز کسی سوخت نمی‌شود.

---

## چیزهایی که آگاهانه انجام ندادم

- **رمزهای plaintext در جدول `stylists`**: ستون `password` را نگه داشتم تا حالت
  دمو نشکند، ولی منبع حقیقت `auth.users` است. توصیه: بعد از این‌که همه آرایشگرها
  حساب واقعی ساختند، `update public.stylists set password = '';`
- **تخفیف پلکانی خودکار روی قیمت**: زیرساخت هست، اعمالش روی `final_price` نیاز
  به تصمیم تو دارد (روی قیمت اصلی یا بعد از تخفیف خدمت؟).
- **پنل مشتری**: تب چهارم لازم دارد، فاز ۴ است.
- **Tailwind**: ببین بالا.

---

## تکمیل شد (پس از تایید): نمایش نرخ بازگشت کمپین در BI + پنل باشگاه مشتریان

**نرخ بازگشت کمپین در تب هوش تجاری** — یک کارت جدید (`CampaignReturnRateCard`) بالای
داشبورد BI اضافه شد که مستقیماً از `fetchCampaigns()` می‌خواند و برای هر کمپین
تعداد هدف/ارسال‌شده/بازگشته و درصد بازگشت را نشان می‌دهد. داده از قبل توسط تریگر
دیتابیس محاسبه می‌شد؛ این کارت فقط نمایشش می‌دهد.

**تب «باشگاه مشتریان» (owner/manager-only)** — تب کامل جدید شامل:
- تنظیمات برنامه (امتیاز هر ویزیت، امتیاز هر سطح، تخفیف هر سطح، سقف تخفیف،
  امتیاز معرفی) با ذخیره روی `loyalty_settings` — دو تابع جدید در `api.js`:
  `fetchLoyaltySettings` / `updateLoyaltySettings`
- فهرست مشتریان با امتیاز، سطح، و تخفیف پلکانیِ محاسبه‌شده (مرتب‌شده بر اساس
  امتیاز، قابل جستجو)
- کلیک روی هر مشتری → مودال با تاریخچهٔ کامل تراکنش‌های امتیاز (`customer_loyalty`)
- در حالت دمو (بدون Supabase)، به‌جای کرش، پیام مناسب نشان می‌دهد

**عمداً هنوز انجام‌نشده (طبق نکتهٔ قبلی):** اعمال خودکار تخفیف پلکانی روی
`final_price` نوبت — چون هنوز مشخص نیست باید روی قیمت اصلی خدمت اعمال بشه یا
بعد از تخفیف خودِ خدمت. تب فعلی فقط درصد تخفیف مستحق‌شدهٔ هر مشتری رو *نمایش*
می‌ده، پرسنل باید فعلاً دستی اعمالش کنن.

هیچ کامپوننت دیگری (BookingFlow، DashboardTab، AccountingTab، ...) دست نخورد؛
فقط دو کامپوننت جدید اضافه و در `PanelView` سیم‌کشی شدن.

---

## Phase 5: تفکیک حسابداری/هوش تجاری + سگمنت‌بندی RFM + کمپین‌های هوشمند

**بازطراحی تب حسابداری** — فقط معیارهای مالیِ خالص موندن: درآمد کل، هزینه‌ها،
سود خالص، و یک بخش جدید «نقدینگی به تفکیک روش پرداخت» (نقد/پوز/کارت‌به‌کارت).
دو مورد تحلیلی حذف شدن (به هوش تجاری منتقل شدن): «۳ خدمت پرفروش»،
«پردرآمدترین خدمات»، «مشتریان جدید در برابر بازگشتی».

⚠️ **نکته دربارهٔ نقدینگی**: ستون `payment_method` به جدول `appointments` اضافه
شد، ولی هنوز هیچ نقطه‌ای برای *ثبت* روش پرداخت هنگام تکمیل نوبت وجود نداره —
یعنی همهٔ نوبت‌های فعلی و آینده تا اضافه‌شدن اون بخش، زیر «نامشخص» می‌افتن.
این عمداً خارج از دامنهٔ این فاز نگه داشته شد چون یعنی دست‌زدن به DashboardTab
(جایی که نوبت تکمیل می‌شه) که در دلیوربل این فاز نبود.

**تب هوش تجاری اضافه شد:**
- «۳ خدمت پرفروش» و «پردرآمدترین خدمات» — هر دو با مدال 🥇🥈🥉ی، درصد از کل
- «مشتریان جدید در برابر بازگشتی» — منتقل‌شده از حسابداری، بدون تغییر منطق
- **سگمنت‌بندی RFM** — کارت جدید با ۶ سگمنت (قهرمانان، نیازمند توجه، در خطر
  ریزش، جدید و امیدبخش، خواب‌رفته، پتانسیل وفاداری)، هرکدوم با تعداد و درصد از
  کل. دکمهٔ «ارسال پیامک به این دسته» مستقیم به تب پیامک با همون سگمنت
  پیش‌انتخاب‌شده می‌بره.

**دیتابیس** — `view customer_rfm_raw` (recency/frequency از جدول `customers`،
monetary تازه محاسبه‌شده از `appointments`) + تابع `get_customer_rfm_segments()`
با امتیازدهی دقیقاً طبق قوانین دادهشده (R بر اساس روز، F بر اساس تعداد ویزیت،
M با `ntile(4)`). فقط برای owner/manager قابل اجراست (`is_manager()` داخل
خود کوئری چک می‌شه، نه فقط با grant سطح دیتابیس).

**کمپین‌های هوشمند یک‌کلیکی** — بالای تب پیامک، سه کارت آماده برای سه سگمنتِ
عملیاتی (قهرمانان/نیازمند توجه/در خطر ریزش) با متن دقیقاً طبق نمونهٔ داده‌شده
(لحن صمیمی و خودمانی). هر کارت گیرنده‌ها رو از RFM segment می‌گیره
(بدون `sms_opt_out`، شمارهٔ معتبر)، متغیرهای `{{name}}`/`{{days}}` رو جایگزین
می‌کنه، و با یک کلیک «تایید و ارسال سریع» ارسال می‌شه — همون مکانیزم
`createCampaign`/`addCampaignTargets`/`sendBulkSms` که کمپین جذب مجدد قبلی
استفاده می‌کرد، پس این کمپین‌ها هم توی نرخ بازگشتِ تب BI دیده می‌شن.

هیچ کامپوننت دیگری (BookingFlow، TrackView، DashboardTab، ScheduleTab، ...)
دست نخورد؛ فقط AccountingTab، BITab، و SmsTab.

---

## تست جامع + رفع باگ کد معرفی

یک دور کامل تست تعاملی (نه فقط خواندن کد) روی کل اپ انجام شد: مسیر نوبت‌دهی
هر دو بخش (زنانه/مردانه، مشتری شناخته‌شده و جدید)، ورود مدیر و آرایشگر،
RBAC (تایید شد آرایشگر فقط تب‌های خودش رو می‌بینه)، همهٔ تب‌های پنل، پیگیری
نوبت + لغو/تغییر زمان، حسابداری (تایید بازطراحی فاز ۵)، هوش تجاری، پیامک،
باشگاه مشتریان — جمعاً ۸۲ بررسی، همه پاس.

**باگ واقعی پیدا و رفع شد: کد معرفی کاملاً یتیم بود.** تابع دیتابیسی
`apply_referral()` از قبل کامل و درست پیاده‌سازی شده بود (اعتبارسنجی کد،
جلوگیری از خودارجاعی، جلوگیری از استفادهٔ دوباره، پاداش هر دو طرف) ولی:
- **هیچ فیلدی برای وارد کردن کد معرف** در مسیر نوبت‌دهی مشتری جدید وجود نداشت
- **هیچ‌جا مشتری کد معرفی خودش رو نمی‌دید** تا به کسی بده

هر دو رفع شد:
- `applyReferral()` به `src/lib/api.js` اضافه شد
- فیلد «کد معرفی (اختیاری)» به فرم مشتری جدید در نوبت‌دهی برگشت؛ `addBooking`
  یک callback اختیاری گرفت که بعد از ثبت موفق نوبت (وقتی ردیف مشتری از طریق
  تریگر ساخته شده) کد معرف رو اعمال می‌کنه
- کارت «باشگاه مشتریان شما» در تب «پیگیری نوبت» اضافه شد — تنها جایی که
  مشتری امتیاز فعلی و **کد معرفی خودش** (با دکمهٔ کپی) رو می‌بینه؛ بدون این،
  کل قابلیت معرفی عملاً غیرقابل‌استفاده بود چون هیچ‌کس کدی برای اشتراک‌گذاری
  نداشت

دو باگ کوچک‌تر حین ویرایش پیش اومد و بلافاصله رفع شد: خط تعریف تابع
`TrackView` موقع جابه‌جایی کد یک‌بار پاک شد (مثل باگ مشابه فاز ۵ با
`SmsTab`) — با build واقعی و ESLint گرفته و رفع شد.

---

## بازطراحی ظاهری (رنگ‌بندی + گزارش‌دهی)

⚠️ نکته دربارهٔ لینک اینستاگرام ارسالی: به محتوای اینستاگرام دسترسی مستقیم
ندارم (ربات‌های سایت اجازهٔ دسترسی نمی‌دن)، پس این بازطراحی رو بر اساس
الگوهای رایج و شناخته‌شدهٔ داشبوردهای مدرن (گرادیان، شیشه‌ای/glass، تایپوگرافی
درشت) انجام دادم، نه با دیدن مستقیم اون ریلز خاص.

**توکن‌های رنگی:** طیف accent غنی‌تر شد، گرادیان‌های برند/success/danger/info/
warning/زنانه/مردانه اضافه شدن (`--grad-*`)، سایه‌های نرم‌تر و رنگی
(`--shadow-glow-*`) برای حس عمق، و کلاس `.card-glass` برای کارت‌های شیشه‌ای.

**کارت‌های آماری (StatCard)** — همه‌جای اپ: آیکون داخل یک نشان گرادیانی
رنگی (نه فقط رنگ ساده)، عدد بزرگ‌تر و پررنگ‌تر، روند به‌شکل پیل رنگی.

**حسابداری** — کارت اصلی درآمد به یک کارت شیشه‌ای واقعی تبدیل شد (گرادیان
برند، دایرهٔ محو تزیینی، تایپوگرافی درشت‌تر). نمودار میله‌ای روند درآمد
گرادیان و درخشش گرفت.

**هوش تجاری** — ۴ کارت KPI هرکدوم نشان گرادیانی رنگی مخصوص خودش گرفتن
(درآمد=طلایی، بازگشت=سبز، مشتری جدید=آبی، عدم‌حضور=کهربایی) + پیل رنگی برای
درصد تغییر. کارت سگمنت‌بندی RFM نوار رنگی بالا و نشان بزرگ‌تر گرفت.

بدون تغییر منطق/داده — فقط لایهٔ ظاهری. با build واقعی و ESLint تست شد.

---

## اعمال سند «مقیاس بلوغ هوش تجاری» — پیش‌بینی درآمد واقعی

سند بارگذاری‌شده یک روش پنج‌گامه برای ارزیابی بلوغ BI بود (ترکیب مدل TDWI،
نردبان چهارسطحیِ تحلیل، و ماتریس ارزش/دشواری گارتنر). این روش رو واقعاً
روی وضعیت فعلی اپ اجرا کردم:

**ارزیابی:**
- گام ۱ (شمارش داده‌ها): همه‌چیز در یک دیتابیس Supabase یکپارچه — نه پراکنده
- گام ۲ (سطح TDWI): «نوجوانی» — گزارش‌دهی خودکار (حسابداری، BI) + شروع
  تصمیم‌سازی داده‌محور (کمپین‌های هدفمند بر پایهٔ RFM)
- گام ۳ (نردبان تحلیل): «توصیفی» کاملاً پوشش داده شده؛ «تشخیصی» تا حدی (drill-down
  دستی)؛ «پیش‌بینانه» و «تجویزی» خالی بودن — دقیقاً همون‌جا که تب «تحلیل
  هوشمند» قبلاً «به‌زودی» نشون می‌داد
- گام ۴ (ماتریس گارتنر): از بین ۳ گزینهٔ کاندید (پیش‌بینی درآمد، پیش‌بینی
  ساعات پرترافیک، تحلیل خودکار علت افت/رشد)، «پیش‌بینی درآمد» ارزش بالا و
  دشواری پایین داشت — چون کاملاً از داده‌های موجود، بدون جمع‌آوری چیز
  جدیدی، قابل محاسبه بود
- گام ۵ (قدم بعدی): همین، پیاده‌سازی شد

**پیاده‌سازی واقعی:** تب «تحلیل هوشمند» یک ویجت پیش‌بینی درآمد واقعی گرفت —
رگرسیون خطی حداقل مربعات روی ماه‌های کامل گذشته (حداقل ۳ ماه لازمه، وگرنه
پیام محترمانهٔ «هنوز اول مسیریم» نشون داده می‌شه)، با بازهٔ اطمینان (±انحراف
معیار باقیمانده‌ها) و نمودار میله‌ای روند. کاملاً سمت کلاینت، بدون مدل
جعبه‌سیاه. آیتم «پیش‌بینی درآمد ماه آینده» از لیست «برنامه‌ریزی‌شده» حذف شد
چون واقعاً ساخته شد؛ ۳ آیتم دیگر (ساعات پرترافیک، قیمت‌گذاری، ریزش مشتری)
هنوز در انتظارند.

با ۹ تست واحد جداگانه روی خود محاسبه (داده خالی، داده ناکافی، روند صعودی،
روند صاف، فیلتر صحیح نوبت‌های لغوشده/در انتظار) و یک تست تعاملی کامل روی
خود اپ تایید شد.

---

## رفع ۵ مشکل گزارش‌شده

**۱. باگ زمان بیکار بین نوبت‌ها (رفع شد)** — وقتی سرویس ۲۰ دقیقه‌ای بود
(مثلاً ۸:۰۰ تا ۸:۲۰) و شبکهٔ زمانی هر ۱۵ دقیقه بود، اسلات بعدی به‌جای ۸:۲۰
مستقیم می‌رفت روی ۸:۳۰ — چون فقط نقاط شبکهٔ ۱۵دقیقه‌ای (۸:۰۰،۸:۱۵،۸:۳۰...)
به‌عنوان گزینه در نظر گرفته می‌شدن، نه لحظهٔ واقعی آزاد شدن نوبت قبلی. حالا
علاوه بر شبکهٔ ثابت، لحظهٔ دقیق پایان هر نوبت موجود (به‌علاوهٔ بافر آن) هم
به‌عنوان یک گزینهٔ زمانی اضافه می‌شه — یعنی ۸:۲۰ الان قابل‌انتخابه. با ۹ تست
واحد (شامل بازتولید دقیق باگ گزارش‌شده، رفتار با/بدون بافر، و عدم تکرار
زمان) تایید شد. در هر دو مسیر (رزرو مشتری و تغییر‌زمان توسط پرسنل) اعمال شد.

**۲. عنوان بزرگ‌تر برای هر داشبورد در حسابداری (انجام شد)** — عنوان کارت‌های
«درآمد تحقق‌یافته»، «نرخ تکمیل نوبت‌ها»، «روند درآمد»، «هزینه‌های این بازه»،
و «درآمد هر آرایشگر» همه به ۱۵px و وزن ۸۰۰ بزرگ‌تر شدن، بالای هر کارت.

**۳. حذف دو داشبورد** — «سهم هر بخش از درآمد» و «نقدینگی به تفکیک روش
پرداخت» کاملاً حذف شدن (همراه محاسبات مرتبط‌شون).

**۴. درآمد هر آرایشگر (اضافه شد)** — داشبورد جدید، فقط برای مدیر سالن
(آرایشگرها این رو نمی‌بینن چون خودشون فقط عدد خودشونو می‌بینن)، نمودار
میله‌ای درآمد هر آرایشگر به تفکیک، مرتب‌شده از بیشترین به کمترین.

**۵. محدودیت ۳ مورد برای پرفروش‌ترین/پردرآمدترین** — این از قبل توی تب
«هوش تجاری» درست بود (`.slice(0, 3)` روی هر دو لیست). دوباره چک شد و تایید
شد که همینه.

با build واقعی، ESLint، و تست تعاملی زنده روی خود اپ تایید شد.

---

## رفع ابهام «آرایشگر دلخواه» (فرقی ندارد)

**مشکل واقعی:** وقتی مشتری «فرقی ندارد» رو انتخاب می‌کرد، سیستم تمام بخش
(زنانه یا مردانه) رو به‌عنوان **یک تقویم مشترک** در نظر می‌گرفت — یعنی اگه
یک آرایشگر مشغول بود، اون ساعت برای «فرقی ندارد» هم بسته نشون داده می‌شد،
حتی اگه آرایشگر دیگه‌ای کاملاً آزاد بود. بدتر از اون، وقتی نوبت با «فرقی
ندارد» ثبت می‌شد، **هیچ‌وقت آرایشگر واقعی تخصیص داده نمی‌شد** — نوبت با
`staff_id: null` برای همیشه می‌موند.

**رفع شد:**
- «فرقی ندارد» حالا واقعاً **اتحاد زمان‌های خالی همهٔ آرایشگرهای فعال اون
  بخش** رو نشون می‌ده — یک ساعت فقط وقتی غیرقابل‌انتخابه که *همهٔ*
  آرایشگرها اون لحظه مشغول باشن، نه فقط یکی
- به‌محض انتخاب ساعت، سیستم **بلافاصله یک آرایشگر واقعی رو خودکار تخصیص
  می‌ده** (کسی که اون ساعت آزاده؛ در صورت چند گزینه، کسی که اون روز نوبت
  کمتری داره انتخاب می‌شه) و بلافاصله به مشتری نشون می‌ده: «در این ساعت،
  [نام] برای شما در دسترس است»
- متن «فرقی ندارد» شفاف‌تر شد: «بهترین زمان خالی بین همهٔ آرایشگرهای این
  بخش را نشان می‌دهیم؛ پس از انتخاب ساعت، آرایشگر به‌طور خودکار تعیین
  می‌شود»
- صفحهٔ «تایید نهایی» و صفحهٔ نتیجه (کد پیگیری) هر دو حالا همیشه نام
  آرایشگرِ تخصیص‌یافته رو نشون می‌دن — قبلاً فقط وقتی مشتری از اول یک
  آرایشگر مشخص رو انتخاب کرده بود نشون داده می‌شد

با ۱۲ بررسی تعاملی زنده تایید شد: یک آرایشگر رو مشغول کردم، بعد با «فرقی
ندارد» همون ساعت رو دوباره امتحان کردم — درست بود که همچنان قابل‌رزرو بود
و آرایشگر آزادِ دیگه به‌طور خودکار تخصیص پیدا کرد (نه اون آرایشگر مشغول).
همچنین تایید شد که انتخاب مستقیم همون آرایشگر مشغول، هنوز درست همون ساعت
رو بسته نشون می‌ده (بدون رگرسیون).

---

## اجرای پرامپت ۵ فازی — با تطبیق بر واقعیت پروژه، نه کورکورانه

⚠️ **قبل از خوندن جزئیات:** این پرامپت ظاهراً بدون دیدن کد واقعی نوشته شده
بود. فاز ۴ (داشبورد BI) با کلاس‌های Tailwind نوشته شده بود ولی **این پروژه
اصلاً Tailwind نداره** (همه‌چیز inline style + CSS variables است)، و محتواش
هم تا حد زیادی با تب «هوش تجاری» موجود همپوشانی داشت. به‌جای ساختن یک
کامپوننت موازی بی‌استایل، تحلیل‌های واقعاً جدیدش رو **داخل BITab موجود**،
با استایل واقعی پروژه، اضافه کردم. جزئیات کامل در گفتگو ثبت شده.

### فاز ۱ — زیرساخت
- `supabase.js`: تشخیص آفلاین (`isOffline`)، ردیابی آخرین خطا
  (`getLastError`)، و `withRetry()` با exponential backoff + timeout ۱۵ ثانیه
  — کاملاً افزایشی، منطق حالت دمو دست‌نخورده موند
- `api.js`: کش حافظه‌ای TTL=۳۰ثانیه، ولی **فقط روی خواندن‌های غیرحساس**
  (مشتریان، کمپین‌ها، RFM، الگوهای پیامک، تنظیمات وفاداری) — **عمداً هرگز
  روی نوبت‌ها/زمان‌بندی**، چون اون‌ها realtime sync دارن و کش کردنشون دقیقاً
  همون سناریوی دوبار-رزرو-شدن رو می‌سازه
- `src/hooks/useData.js`: هوک جدید مستقل (هنوز داخل کامپوننت‌های موجود
  سیم‌کشی نشده — چون خودش یه ریفکتور جداست)

### فاز ۲ — امنیت
- `auth.js`: `getCurrentUser`, `getSession`, `onAuthStateChange` اضافه شد
  (بقیه از قبل بود)
- `supabase/migrations/v2.1_security.sql`: فقط چیزهای واقعاً جدید —
  جلوگیری از ویرایش نوبت گذشته توسط آرایشگر، policy ویرایش پروفایل خود
  کاربر (قبلاً اصلاً نبود)، تریگر جلوگیری از لغو ۲۴ساعت‌مانده

### فاز ۳ — PWA
`manifest.json` (با رنگ برند واقعی #c2703a، نه بنفش دلخواه پرامپت)، آیکون‌های
واقعی PNG ۱۹۲/۵۱۲، `sw.js` (که **هیچ‌وقت درخواست‌های Supabase رو کش
نمی‌کنه** — فقط asset های همان‌مبدأ)، `offline.html`، متاتگ‌ها، Error
Boundary + بنر آفلاین در `main.jsx`، `vite.config.js` (terser، chunk
splitting برای react/supabase جدا).

### فاز ۴ — تحلیل‌های جدید در BITab موجود
۵ بخش جدید، همه با استایل واقعی پروژه:
- **ساعات پرترافیک** — نمودار میله‌ای نوبت‌ها بر ساعت (۸ تا ۲۰)
- **روزهای شلوغ هفته** — نمودار میله‌ای شنبه تا جمعه
- **جدول عملکرد آرایشگران** — قابل‌مرتب‌سازی با کلیک روی ستون، رنگ‌بندی
  شرطی (سبز/قرمز نسبت به میانگین)، امتیاز ترکیبی (۴۰٪ تعداد + ۳۵٪ میانگین
  درآمد + ۲۵٪ نرخ بازگشت)
- **مشتریان برتر (پارتو)** — چند درصد مشتری چند درصد درآمد رو ساختن
- **پیش‌بینی تقاضا** — میانگین متحرک ۷روزه + مدل ظرفیت **دقیق** (نه تقریبی)
  برای ۷ روز آینده، بر پایهٔ ساعات کاری واقعی هر آرایشگر (با احتساب مرخصی و
  تاییدشدن روز)، با هشدار برای روزهای با پرشدگی زیر ۲۰٪

### فاز ۵ — CI/CD
`.github/workflows/deploy.yml` (build + دیپلوی Vercel + دیپلوی Edge
Functions، با env secrets)، و در `package.json`: `lint`, `test`, `analyze`,
`db:backup` + `vitest`, `@testing-library/react`, `eslint`, `terser`.

### تست
build واقعی + ESLint + چک آیکون‌ها، و تست تعاملی کامل: بوکینگ واقعی سه نوبت
و تایید اینکه نرخ پرشدگی پیش‌بینی تقاضا از ۰٪ به ۶٪ درست تغییر کرد (یعنی
مدل ظرفیت واقعاً کار می‌کنه، نه فقط رندر بدون خطا).

---

## بررسی امنیتی — رفع آسیب‌پذیری‌های واقعی (نه گزارش عمومی Firebase/Express)

سند ورودی یه گزارش امنیتی ۳۶موردی بود که فرض کرده بود پشته Firebase + Express
+ کیف‌پول/پرداخت داره. **هیچ‌کدوم اینا توی این پروژه نیست** (Supabase +
React + Edge Function، بدون wallet). موارد زیر بر این اساس ارزیابی شدن:
کاملاً غیرقابل‌اجرا (Firebase، wallet/payment: C1، C5، M2، M5، M7، M12،
M13)، از قبل امن (تایید شد)، یا واقعاً پیدا و رفع شد.

### 🔴 بحرانی — پیدا شده حین بررسی (فراتر از خودِ گزارش)

**آپدیت نامحدود نوبت توسط کاربر ناشناس** — سیاست RLS برای `auth.uid() is
null` (لازم برای اینکه مشتری بدون لاگین نوبتش رو لغو/جابه‌جا کنه) هیچ
محدودیتی نداشت — یعنی هرکسی می‌تونست **هر نوبتی** رو به **هر مقداری** تغییر
بده: نوبت غریبه رو «تکمیل‌شده» بزنه (امتیاز وفاداری تقلبی)، آرایشگر/قیمتش
رو عوض کنه، یا شمارهٔ تماسش رو بازنویسی کنه.
- **فایل:** `supabase/schema.sql` (خط ~۴۰۷) + `supabase/migrations/v2.2_critical_fixes.sql`
- **رفع:** `WITH CHECK` که فقط `status in ('cancelled','rescheduled')` رو
  اجازه می‌ده + تریگر `guard_anonymous_appt_update` که فیلدهای حساس
  (تلفن، قیمت، آرایشگر) رو برای کاربر ناشناس کاملاً قفل می‌کنه
- **تست:** با کلاینت ناشناس (anon key، بدون session) سعی کن نوبت یه مشتری
  دیگه رو `status: 'completed'` کنی → باید با خطای RLS رد بشه

### 🔴 بحرانی ≈ H1 — رمز عبور متنی

فرم «ویرایش آرایشگر» رمز رو متنی ذخیره و در input قابل‌مشاهده نمایش می‌داد
— با اینکه ورود واقعی از قبل Supabase Auth (bcrypt) بود و این فیلد **هیچ
کاربرد واقعی نداشت** (ثبت‌نام واقعی آرایشگر از یک مسیر کاملاً جدا و
خودکار انجام می‌شه).
- **فایل‌ها:** `supabase/schema.sql`، `src/lib/api.js`، `src/App.jsx` (`StylistEditModal`)
- **رفع:** ستون از دیتابیس حذف شد؛ مپر API دیگه نه می‌خونه نه می‌نویسه؛
  فرم فقط در حالت دمو رمز می‌گیره، در حالت واقعی توضیح می‌ده رمز از مسیر
  «ورود آرایشگر ← ثبت‌نام» تنظیم می‌شه
- **تست:** یک آرایشگر جدید در حالت واقعی اضافه کن، بعد مستقیم دیتابیس رو
  چک کن — ستون `password` اصلاً نباید وجود داشته باشه

### 🟠 HIGH

| ID | مشکل | رفع | تست |
|---|---|---|---|
| H4 | بدون idempotency روی ارسال پیامک | ستون `idempotency_key` + چک قبل از ارسال در `send-sms`؛ کلاینت برای پیامک‌های خودکار کلید قطعی می‌سازه (`kind-appointmentId`) | همون پیام رو دوبار با یک کلید بفرست → باید فقط یک‌بار واقعاً ارسال بشه |
| H5 | خطای خام دیتابیس به کاربر | در `send-sms`، پیام عمومی برگردونده می‌شه، جزئیات فقط `console.error` سرور | یک درخواست خراب بفرست، پاسخ نباید شامل متن خطای Postgres باشه |
| H6 | تقلب در معرفی دوستان | `apply_referral` حالا: شمارهٔ معرفی‌شده باید مشتری واقعی باشه (نوبت واقعی داشته) + محدودیت ۵ پاداش در روز برای هر معرف | با یک شمارهٔ ساختگی (بدون نوبت) تلاش کن از کد معرف استفاده کنی → باید رد بشه |
| H7 | CORS پیش‌فرض باز (`*`) | اگه `ALLOWED_ORIGIN` تنظیم نشه، هیچ هدر CORS فرستاده نمی‌شه (fail-closed) | بدون تنظیم متغیر، هدر `Access-Control-Allow-Origin` نباید در پاسخ باشه |

### 🟡 MEDIUM

| ID | مشکل | رفع | تست |
|---|---|---|---|
| M1 | کد پیگیری با `Math.random()` | `crypto.getRandomValues()` با fallback ایمن | چند نوبت پشت‌سرهم بساز، کدها باید غیرقابل‌پیش‌بینی باشن (نه صرفاً منحصربه‌فرد) |
| M8 | `cron-reminders` بدون احراز هویت | همون رمز مشترک `send-sms` (`x-internal-secret`) لازمه | درخواست بدون هدر بزن → باید ۴۰۱ برگرده |
| L6 (مرتبط) | بدون audit log | جدول `audit_log` جدید + تریگر برای تغییر نقش و آپدیت خودکار نوبت توسط مشتری، فقط مدیر می‌تونه بخونه | نقش یک کاربر رو عوض کن، بعد `select * from audit_log` به‌عنوان مدیر |

### 🟢 LOW

| ID | مشکل | رفع |
|---|---|---|
| L2 | بدون هدر امنیتی | `vercel.json` جدید: HSTS، CSP (با hash دقیق برای اسکریپت inline، نه `unsafe-inline`)، X-Frame-Options، X-Content-Type-Options |
| L1 | کش‌شدن پاسخ‌های حساس | `Cache-Control: no-store` روی `index.html` |
| L7 | بدون timeout نشست | Idle timeout ۱۵ دقیقه + سقف مطلق ۸ ساعت، فقط برای نشست واقعی (نه حالت دمو)، در `src/main.jsx` |

### ✅ از قبل امن (بررسی و تایید شد)
- **C2 تغییر نقش** — از قبل با RLS محدود بود (v2.1)
- **C6 هدف‌گذاری کمپین** — از قبل فقط مدیر (`p_mgr_write` روی `campaigns`/`campaign_targets`)
- **رمز عبور اصلی** — از همون اول Supabase Auth (bcrypt)، هیچ‌وقت خودمون هش نکردیم
- **M10 خطای خام در فرانت‌اند** — بررسی شد، هیچ‌جا `error.message` مستقیم به کاربر نشون داده نمی‌شه

### ❌ غیرقابل‌اجرا (تکنولوژی وجود ندارد)
C1، C5، M2، M5، M6، M7، M12، M13 و هر مورد Firebase/wallet/payment — این
پروژه Firebase یا هیچ قابلیت کیف‌پول/پرداختی نداره.

### نکتهٔ باقی‌مانده
`style-src 'unsafe-inline'` در CSP نگه داشته شد چون کل سیستم استایل‌دهی
اپ (از اول پروژه) روی inline style + یک تگ `<style>` تزریقی بناست —
حذفش یعنی بازنویسی کامل معماری استایل، خارج از دامنهٔ یک پچ امنیتی. ریسکش
هم به‌مراتب کمتر از `script-src unsafe-inline` است.

با build واقعی، ESLint، بررسی TypeScript (تا حد ممکن بدون Deno نصب‌شده)،
و تست تعاملی زنده تایید شد.

---

## رفع باگ: تاریخ روزها در نوبت‌دهی مطابق تقویم شمسی نبود

**مشکل واقعی:** عدد بزرگ روی هر کارت تاریخ (نوار انتخاب روز، هم در مسیر
نوبت‌دهی مشتری، هم در «روزهای باز برای رزرو» تب ساعات کاری، هم در برچسب
پیش‌بینی تقاضای تب هوش تجاری) از `date.getDate()` می‌اومد — که عدد روزِ
**میلادی** رو برمی‌گردونه، نه شمسی! یعنی حرف روز هفته («ش»، «ی»، ...) درست
بود، ولی عدد زیرش روز میلادی بود، در حالی که بقیهٔ اپ (ماه، سال) همه‌جا شمسی
نشون می‌ده — یه ناهماهنگی گیج‌کننده و واقعی.

**رفع شد:** یک تابع کمکی `jalaliDayNum(date)` اضافه شد که فقط عدد روزِ شمسی
رو برمی‌گردونه (با استفاده از همون `gregorianToJalali` که قبلاً بود و دقتش
تایید شده)، و هر ۳ جایی که `toFa(d.getDate())` اشتباه استفاده شده بود، با
`toFa(jalaliDayNum(d))` جایگزین شد.

**تست:** یه تست دقیق نوشتم که هر کارت تاریخ (باز یا بسته) رو با محاسبهٔ
مستقل شمسی مقایسه می‌کنه — ۱۲ روز باز، همه دقیقاً مطابق. حین این تست یک بار
هم خودم اشتباه کردم (انتخابگر تستم روزهای تعطیل/جمعه رو نادیده می‌گرفت و
باعث جابه‌جایی اندیس می‌شد) — که خودش رو هم پیدا و اصلاح کردم تا مطمئن بشم
مشکل واقعاً از اپ بود، نه از تست.

---

## بازطراحی موبایلی — «حس اپ واقعی» به‌جای «حس وب‌سایت»

**تغییر اصلی: نوار ناوبری پایین (Bottom Nav)** — ۳ تب اصلی (نوبت‌دهی/پیگیری
نوبت/پنل مدیریت) که قبلاً یه سگمنت‌کنترل بالای هدر بودن، الان یه نوار
شیشه‌ای (backdrop-blur) با آیکون + برچسب، **ثابت به پایین صفحه**، دقیقاً
همون الگویی که اینستاگرام/اوبر/اپ‌های بانکی استفاده می‌کنن — تنها چیزی که
واقعاً یه سایت رو از یه اپ موبایل متمایز می‌کنه.

**Bottom Sheet واقعی برای همهٔ مودال‌ها** — همهٔ فرم‌ها (افزودن خدمت،
ویرایش آرایشگر، ثبت هزینه، لغو نوبت، ...) از قبل از پایین باز می‌شدن، ولی
الان:
- یه **دستگیرهٔ کوچیک** (grab handle) بالای هر شیت، دقیقاً مثل اپ‌های iOS/Android
- **انیمیشن اسلاید-آپ واقعی** (نه فقط fade)، با easing طبیعی
- **پدینگ safe-area** پایین، برای گوشی‌های دارای دستهٔ پایین (iPhone)
- گوشه‌های بالایی بزرگ‌تر و سایهٔ عمیق‌تر برای حس «شناور روی صفحه»

**هدر سبک‌تر و چسبان (sticky + blur)** — لوگو گرادیانی، بدون تب‌های تکراری
(چون الان پایین صفحه‌ن)، هنگام اسکرول با پس‌زمینهٔ محو (backdrop-blur) شفاف
می‌مونه به‌جای اینکه محتوا زیرش رد بشه.

**پشتیبانی کامل safe-area** — نوار پایین و مودال‌ها، پدینگ `env(safe-area-inset-bottom)`
گرفتن تا پشت دستهٔ پایین گوشی‌های نچ‌دار قایم نشن؛ محتوای اصلی هم پدینگ
پایین کافی داره که پشت نوار ثابت گم نشه.

با ۲۷ بررسی تعاملی (نوار پایین، سوییچ تب، بازشدن شیت‌ها، هندل، کل مسیر
نوبت‌دهی، همهٔ ۹ تب پنل) و build واقعی تایید شد — هیچ رگرسیونی نیومد.

---

## فونت‌ها — جذاب‌تر و درست‌تر

صادقانه: دور قبلی (بازطراحی موبایلی) اصلاً دست به فونت نزدم، فقط ساختار
(نوار پایین، شیت‌ها) رو عوض کردم. این دور واقعاً بهش رسیدگی شد:

**یه باگ واقعی پیدا شد و رفع شد:** فونت Vazirmatn با یه `useEffect` توی
جاوااسکریپت لود می‌شد — یعنی صفحه همیشه اول با فونت پیش‌فرض سیستم رندر
می‌شد، بعد از اجرای افکت و دانلود فونت، یهو عوض می‌شد (یه "چشمک" آزاردهنده،
FOUC). الان مستقیم توی `index.html` با `<link>` لود می‌شه — از همون اول،
موازی با بقیهٔ چیزها، بدون چشمک‌زدن. یه `preconnect` هم اضافه شد که
اتصال به CDN فونت زودتر شروع بشه.

**یه باگ امنیتی پنهان هم همین‌جا پیدا شد:** توی پَس قبلی (بررسی امنیتی)
CSP رو برای `fonts.googleapis.com`/`fonts.gstatic.com` تنظیم کرده بودم —
ولی فونت واقعاً از `cdn.jsdelivr.net` لود می‌شه! یعنی اگه CSP واقعاً توی
مرورگر اجرا می‌شد، فونت اصلاً لود نمی‌شد (چون jsdom که باهاش تست می‌کنم
اصلاً CSP رو اجرا نمی‌کنه، این باگ تا الان دیده نشده بود). رفعش کردم —
CSP الان دقیقاً همون دامنه‌ای رو اجازه می‌ده که واقعاً استفاده می‌شه.

**سلسله‌مراتب تایپوگرافی واقعی:** قبلاً همهٔ تیترها (h1 تا h4) دقیقاً یک
وزن (۷۰۰) داشتن — یعنی هیچ تمایزی نبود. الان:
- h1: وزن ۸۰۰ + فاصلهٔ حروف کمی فشرده‌تر (برای تیترهای اصلی، حس قوی‌تر)
- h2: وزن ۸۰۰، کمی سبک‌تر از نظر فاصلهٔ حروف
- h3: وزن ۷۰۰ (همون قبلی)
- h4: وزن ۶۰۰ (سبک‌تر، برای زیرعنوان‌ها)

چون این تغییر روی قانون CSS مشترکه، همه‌جای اپ خودکار اعمال می‌شه — بدون
نیاز به دست‌کاری تک‌تک کامپوننت‌ها.

با ۱۱ بررسی (۷ تا مخصوص فونت + ۴ تا رگرسیون کامل) و build واقعی تایید شد.

---

## رفع باگ ورود مدیر + برگشت تب‌ها به بالا + سیستم باشگاه مشتریان کامل‌تر

### ۱. باگ «ورود مدیر خطا می‌دهد» — پیدا و رفع شد
**علت واقعی:** این حساب‌ها ایمیل ساختگی (`@salon.local`) دارن. اگه تنظیم
`Confirm email` توی Supabase روشن باشه، بعد از ثبت‌نام session فعالی ساخته
نمی‌شه، و ساخت ردیف پروفایل (که به `auth.uid()` نیاز داره) بی‌صدا شکست
می‌خوره — حساب Auth ساخته می‌شه، پروفایل نه، و ورود همیشه «حساب پیدا
نشد» می‌ده.

**رفع شد:**
- `signIn`/`loadProfile` در `src/lib/auth.js`: حالا اگه ورود موفق بشه ولی
  پروفایل نباشه، **خودش ترمیمش می‌کنه** (self-heal) — دیگه نیازی به SQL
  دستی نیست، فقط دوباره وارد شو
- پیام خطای دقیق‌تر وقتی «Confirm email» علت واقعیه
- اسکریپت SQL جدید (`supabase/migrations/repair_owner_login.sql`) برای رفع
  فوری حساب گیرکردهٔ فعلی، بدون نیاز به صبر برای دیپلوی
- `DEPLOY.md` یه هشدار واضح گرفت، دقیقاً همون‌جایی که تنظیم Confirm email
  توضیح داده می‌شه
- یه باگ واقعی دیگه هم پیدا شد: `registerStylist` هنوز سعی می‌کرد ستون
  `password` (که قبلاً بابت امنیت حذف شده بود) رو بنویسه — رفع شد

### ۲. تب‌ها برگشتن به بالا
نوار پایین که قبلاً ساخته بودم کامل حذف شد. تب‌ها با همون استایل قبلی
(بهبودیافته با آیکون) به هدر برگشتن. `maxWidth: 480` دست‌نخورده موند.

### ۳. «پیگیری نوبت» → «داشبورد من»
تب تغییر نام داد. الان مشتری وقتی شمارهٔ خودش رو وارد می‌کنه می‌بینه:
- نوبت‌هاش (مثل قبل)
- **جدول کامل امتیازات** (تاریخچهٔ هر تراکنش: ویزیت/معرفی/استفاده از تخفیف)
- **زیرمجموعهٔ معرفی‌شده‌هاش** (چه کسانی با کد او ثبت‌نام کردن)
- وقتی به سقف تخفیف برسه، یه پیام واضح می‌بینه

### ۴. امتیاز معرفی — زیاد شد
`referral_points` از ۵۰ به **۳۰۰** (۳۰ برابر امتیاز یک ویزیت) — هم در
`schema.sql` هم در migration جدید (`v2.3_loyalty_upgrades.sql`).

### ۵. تخفیف قابل‌تنظیم + قابل‌مشاهده برای مشتری
تنظیمات تخفیف (امتیاز هر سطح، تخفیف هر سطح، سقف تخفیف) از قبل قابل‌تنظیم
بودن ولی فقط برای مدیر — الان **تب «باشگاه مشتریان» برای آرایشگر هم باز
شد**، طبق درخواست صریح. تخفیف فعلی مشتری الان واضح توی «داشبورد من» نشون
داده می‌شه.

### ۶. سقف امتیاز = تخفیف ۱۰۰٪، با صفر شدن بعد از استفاده
- `max_discount` از قبل یک عدد آزاد بود (می‌تونه ۱۰۰ هم بشه) — تایید و تست شد
- تابع جدید `redeem_loyalty_reward()`: وقتی مشتری به سقف تخفیف رسید و
  آرایشگر/مدیر توی «باشگاه مشتریان» روی «ثبت استفاده از تخفیف» بزنه،
  **امتیاز خودکار صفر می‌شه** و با یه تراکنش منفی در تاریخچه ثبت می‌شه
  (هیچ‌وقت بی‌صدا پاک نمی‌شه)
- هم توی پنل مدیریت (کارت «به سقف تخفیف رسیده») هم توی داشبورد مشتری،
  این وضعیت واضح نشون داده می‌شه

با ۱۶ بررسی تعاملی (تب‌های بالا، تغییر نام، RBAC باشگاه مشتریان، خروج
async-aware، رگرسیون کامل نوبت‌دهی) + ۷ تست واحد روی محاسبات تخفیف/سقف
(شامل سناریوی سقف ۱۰۰٪) + build واقعی تایید شد.

---

## عنوان درشت بالای کادر حسابداری + انتقال سگمنت‌بندی مشتریان

### ۱. عنوان قسمت‌های حسابداری
بررسی کردم — همهٔ کارت‌های حسابداری از قبل عنوان درشت (۱۵px/۸۰۰) داشتن،
**به‌جز یکی**: کارت «نرخ تکمیل نوبت‌ها» عنوانش رو کنار نمودار دایره‌ای
(به‌صورت افقی و چسبیده) نشون می‌داد، نه به‌صورت یک تیتر مستقل بالای کادر.
رفعش کردم — الان عنوان اول، تمام‌عرض، بالای کادر میاد؛ نمودار و توضیحات
زیرش.

### ۲. سگمنت‌بندی مشتریان (RFM) منتقل شد
از تب «هوش تجاری» به تب «باشگاه مشتریان» منتقل شد. نکتهٔ مهم: خودِ تابع
دیتابیسی این ویژگی (`get_customer_rfm_segments`) از قبل فقط برای
owner/manager محدود بود (`is_manager()` داخل خود کوئری) — برای همین حتی با
اینکه بقیهٔ تب «باشگاه مشتریان» الان برای آرایشگر هم بازه، این بخش خاص
همچنان فقط برای مدیر نشون داده می‌شه (سازگار با محدودیت واقعی دیتابیس).

**قابلیت جدید:** هر دسته الان قابل‌کلیک شده — با زدن روش، یک پنجره باز
می‌شه که اعضای همون دسته رو نشون می‌ده، **مرتب‌شده بر اساس بیشترین درآمد**
(باارزش‌ترین عضو اول)، همراه با تعداد ویزیت و روزهای گذشته از آخرین
مراجعه. دکمهٔ «ارسال پیامک به این دسته» هم داخل همون پنجره موجوده.

با ۱۶ بررسی تعاملی (تیتر حسابداری، حذف از BI، وجود در باشگاه مشتریان،
رگرسیون کامل نوبت‌دهی و همهٔ تب‌ها) + ۵ تست واحد روی منطق مرتب‌سازی اعضا
(شامل حالت مقدار خالی) + build واقعی تایید شد.

---

## بازطراحی حرفه‌ای «داشبورد من» + رنگ‌بندی متنوع‌تر تب‌ها

### داشبورد من — از انباشتگی به ساختار منظم
قبلاً همه‌چیز (امتیاز، کد معرفی، تاریخچه، زیرمجموعه، نوبت‌ها) زیر هم روی
هم انباشته می‌شد. الان:

- **کارت خوش‌آمدگویی** با گرادیان فیروزه‌ای — سلام با اسم مشتری (اگه
  ثبت شده باشه)، امتیاز درشت، نشان‌های تخفیف/تعداد نوبت/تعداد معرفی، و کد
  معرفی، همه در یک نگاه اول
- **سه بخش با سوییچ مجزا** به‌جای اسکرول بی‌پایان: «نوبت‌ها» · «امتیازات» ·
  «معرفی‌ها» — هرکدوم با رنگ و آیکون مخصوص خودش، همراه شمارندهٔ تعداد
- **جدول امتیازات بازطراحی‌شده**: هر تراکنش با آیکون جهت‌دار (سبز/قرمز) و
  تاریخ شمسی، نه فقط عدد خام

### زیرمجموعه‌ها با اسم کامل و مشخص
هر عضو معرفی‌شده الان:
- **آواتار رنگی** (حرف اول اسم، رنگ ثابت و همیشگی برای همون شخص — نه
  تصادفی هر بار)
- **اسم کامل با فونت درشت (۱۴px/۸۰۰)** به‌عنوان عنصر اصلی ردیف — دیگه
  متن کوچیک خاکستری نیست
- تعداد ویزیت + تاریخ عضویت، به‌صورت واضح زیر اسم

### رنگ‌بندی تب‌ها — متنوع‌تر، خاکستری کمتر
هر کدوم از ۳ تب اصلی هویت رنگی مستقل گرفت:
- نوبت‌دهی: طلایی (برند اصلی)
- داشبورد من: فیروزه‌ای/آبی تازه
- پنل مدیریت: بنفش/نیلی

حالت غیرفعال هم دیگه خاکستری ساده نیست — هر تب حتی غیرفعال هم ته‌رنگ و
حاشیهٔ مخصوص خودش رو داره، نه رنگ یکسان خاکستری برای همه.

با ۱۳ بررسی تعاملی (رنگ تب‌ها، سوییچ بخش‌ها، رگرسیون کامل نوبت‌دهی) + ۷
تست واحد روی منطق اسم کامل/رنگ آواتار (شامل حالت‌های لبه‌ای) + ۱۳ بررسی
رگرسیون روی همهٔ تب‌های پنل + build واقعی تایید شد.

---

## بازطراحی فوق‌حرفه‌ای همهٔ تب‌ها، مخصوصاً پنل مدیریت

### هدر پنل — از یک خط ساده به یه بنر واقعی
«پنل مدیر سالن» / «پنل شخصی [نام]» الان یه بنر گرادیانی بنفش با آیکون
shield، و دکمهٔ خروج به‌شکل پیل شیشه‌ای — نه یه خط متن ساده با دکمهٔ کوچیک.

### نوار زیرتب‌ها — از چیدمان خاکستری چندردیفه به نوار رنگی افقی
قبلاً ۹ تب روی هم می‌شکستن و چندردیفه می‌شدن، با پس‌زمینهٔ خاکستری یکنواخت.
الان:
- **نوار افقی با اسکرول** (نه شکستن به چند ردیف) — الگوی استاندارد اپ‌های
  حرفه‌ای موبایل برای تعداد زیاد تب
- **هر تب رنگ مخصوص خودش** — نوبت‌های امروز (طلایی)، حسابداری (کهربایی)،
  خدمات (گلبهی)، آرایشگرها (آبی)، ساعات کاری (فیروزه‌ای)، پیامک (بنفش)،
  باشگاه مشتریان (سبز)، تحلیل هوشمند (فیروزه‌ای)، هوش تجاری (بنفش) — حتی
  حالت غیرفعال هم ته‌رنگ خودش رو داره، نه خاکستری یکسان

### هدر اختصاصی برای هر تب
۶ تبی که قبلاً هیچ عنوانی نداشتن و مستقیم می‌رفتن سر محتوا (نوبت‌های
امروز، خدمات، آرایشگرها، ساعات کاری، پیامک، باشگاه مشتریان)، الان یه
هدر واقعی دارن: آیکون رنگی در یک نشان، عنوان درشت، و یک توضیح کوتاه —
دقیقاً هم‌رنگ با آیکون خودشون توی نوار تب‌ها، برای هماهنگی بصری.

با ۳۲ بررسی تعاملی (نوار جدید، ۹ تب مدیر، ۶ تب آرایشگر با RBAC درست،
رگرسیون کامل نوبت‌دهی و داشبورد مشتری) + build واقعی تایید شد — بدون
هیچ رگرسیونی روی کارهای قبلی.

---

## 🔴 رفع یک باگ بنیادی: کلاس‌های چیدمانی (flex/grid/gap/margin) اصلاً کار نمی‌کردن

### گرید ۳ستونه برای نوار زیرتب‌های پنل
طبق درخواست، نوار زیرتب‌های پنل مدیریت از یک نوار افقیِ قابل‌اسکرول به یک
**گرید ۳ ستونه** تبدیل شد — همهٔ ۹ تب مدیر (یا ۶ تب آرایشگر) الان همزمان و
بدون نیاز به اسکرول، در یک نگاه قابل مشاهده‌ان.

### کشف حین کار: باگ بنیادی در سراسر اپ

موقع ساختن این گرید، متوجه شدم **هیچ‌کدام از کلاس‌های چیدمانی که در ۱۸۶+
جای مختلف کل اپ استفاده شده بودن — `flex`، `grid`، `items-center`،
`justify-between`، `gap-*`، `mb-*`، `mt-*`، `w-full` — هیچ قانون CSS
واقعی نداشتن.** این پروژه Tailwind نصب نداره و هیچ فایل CSS جداگانه‌ای هم
تولید نمی‌کنه (تمام استایل فقط از یک رشتهٔ `TOKENS_CSS` تزریق می‌شه). یعنی
این کلاس‌ها توی نسخهٔ **واقعی دیپلوی‌شده** (نه پیش‌نمایش‌های Claude.ai که
خودشون Tailwind دارن) عملاً هیچ کاری نمی‌کردن — کل چیدمان اپ می‌تونست در
یک دیپلوی واقعی به‌هم‌ریخته دیده بشه.

**چرا تا حالا دیده نشده بود؟** چون همهٔ تست‌های تعاملی قبلی با jsdom انجام
می‌شدن، و jsdom اصلاً موتور محاسبهٔ چیدمان (layout) نداره — فقط کلیک، متن،
و ساختار DOM رو چک می‌کنه، نه اینکه `display:flex` واقعاً اعمال شده یا نه.

**رفع شد:** یه لایهٔ کامل CSS شامل هر ۳۳ کلاس گمشده به `TOKENS_CSS` اضافه
شد (`flex`, `flex-1`, `flex-col`, `flex-wrap`, `grid`, `items-center`,
`items-end`, `items-start`, `justify-between`, `justify-center`,
`w-full`, `mx-auto`, `gap-0.5` تا `gap-4`, `gap-x-3`, `gap-y-1.5`,
`mb-1` تا `mb-6`, `mt-1` تا `mt-5`).

**یه باگ ظریف‌تر هم توی همین رفع پیدا و اصلاح شد:** برای کلاس‌های اعشاری
(`gap-1.5`, `mt-1.5`, ...)، نوشتن `\.` داخل یک template literal جاوااسکریپت
اشتباهه — چون `\.` یک escape sequence ناشناخته برای JS است و بک‌اسلش
بی‌صدا حذف می‌شه، سلکتور CSS می‌شکنه. باید `\\.`  (دو بک‌اسلش) نوشته
می‌شد؛ این هم پیدا و درست شد.

**روش تست این بار:** برخلاف قبل، به‌جای اکتفا به jsdom ساختاری، از
`getComputedStyle()` واقعی برای تایید مستقیم استفاده شد:
- **۴۲۱ عنصر flex/grid** در سراسر اپ (مسیر نوبت‌دهی + همهٔ ۹ تب پنل)
  بررسی شدن — همه درست `display` رو resolve کردن
- **۵۵ نمونهٔ فاصله‌گذاری** (gap/margin) روی صفحهٔ واقعی — صفر مغایرت
- گرید جدید ۳ستونه با `getComputedStyle` تایید شد که واقعاً `display:grid`
  و `grid-template-columns: repeat(3, 1fr)` محاسبه می‌شه، نه فقط این‌که
  نام کلاس درست باشه
- رگرسیون کامل نوبت‌دهی + داشبورد مشتری + همهٔ تب‌های پنل

این احتمالاً یکی از مهم‌ترین رفع‌های این پروژه بود — بدون این اصلاح، کل
چیدمان اپ در دیپلوی واقعی می‌تونست به‌هم‌ریخته باشه، حتی با اینکه توی
پیش‌نمایش‌های Claude.ai (که خودشون Tailwind دارن) کاملاً درست دیده می‌شد.

---

## تعطیلی ساعتی (بازسازی — قبلاً ساخته شده بود ولی هیچ‌وقت تحویل داده نشد)

در یک نوبت قبلی این ویژگی کامل ساخته و تست شده بود، ولی درست وسط کار روی
درخواست دوم همون پیام، به محدودیت ابزار خوردم و **هیچ‌وقت zip واقعاً
به‌روزرسانی نشد** — یعنی نسخهٔ قبلی که تحویل داده شده بود اصلاً این ویژگی
رو نداشت. این‌بار از نو ساخته و این‌بار واقعاً تحویل داده می‌شه.

**قابلیت:** در فرم «تعطیلی‌های موقت سالن» (تب ساعات کاری)، سوییچ «تمام
روز» / «بازهٔ ساعتی» — امکان بستن فقط بخشی از روز (مثلاً ۱۳:۰۰ تا ۱۴:۰۰
برای استراحت ناهار)، بدون تعطیل کردن کل روز.

## تایید مشتری برای پیشنهاد تغییر زمان آرایشگر

**مشکل قبلی:** وقتی آرایشگر/مدیر روی «تغییر زمان» یک نوبت می‌زد، نوبت
فوراً و بدون اطلاع واقعی مشتری جابه‌جا می‌شد.

**رفع شد — جریان کاملاً جدید:**
1. آرایشگر «تغییر زمان» می‌زنه → به‌جای جابه‌جایی فوری، فقط یک
   **پیشنهاد** ثبت می‌شه (وضعیت: «پیشنهاد تغییر زمان»). نوبت واقعی دست‌نخورده
   می‌مونه تا پاسخ مشتری. یک پیامک برای مشتری ارسال می‌شه.
2. مشتری در «داشبورد من» یه کارت مخصوص می‌بینه: زمان قبلی (خط‌خورده) ←
   زمان پیشنهادی (پررنگ)، با سه گزینه:
   - **تایید تغییر** — نوبت رسماً به زمان جدید منتقل می‌شه
   - **لغو نوبت** — نوبت کلاً لغو می‌شه
   - **انتخاب زمان دیگر** — مشتری خودش از بین زمان‌های آزاد، یک زمان
     کاملاً متفاوت انتخاب می‌کنه (مستقل از پیشنهاد آرایشگر)
3. در همهٔ حالت‌ها، پیامک مناسب برای مشتری ارسال می‌شه.

**فنی:** ستون‌های جدید `pending_date`/`pending_start_min`/`pending_end_min`
+ وضعیت جدید `reschedule_proposed` در جدول `appointments`. تا وقتی پیشنهاد
تایید نشده، نوبت همچنان زمان اصلی خودش رو توی سیستم زمان‌بندی اشغال می‌کنه
(کسی دیگه نمی‌تونه همون ساعت رو رزرو کنه)، ولی زمان پیشنهادی هم آزاده تا
مشتری تصمیم بگیره. هیچ تغییری در RLS لازم نبود — سیاست‌های امنیتی قبلی
(از پَس امنیتی قبلی) دقیقاً همین دو خروجی («لغو» یا «تغییر یافته») رو از
قبل برای مشتری مجاز کرده بودن.

با ۳۷ بررسی تعاملی (هر ۳ مسیر پاسخ مشتری — تایید/لغو/انتخاب دیگر — به‌طور
کامل End-to-End، از پیشنهاد آرایشگر تا پیامک نهایی) + ۱۲ تست واحد روی
منطق تغییر وضعیت + رگرسیون کامل روی همهٔ ۹ تب پنل + build واقعی تایید شد.

---

## بازطراحی حرفه‌ای تب «پیامک»

### قبلاً: یک اسکرول طولانی و شلوغ
همه‌چیز — کمپین‌های هوشمند (۶ کارت)، انتخاب مخاطب (۶ گزینه + دو اسلایدر
برای «جذب مجدد»)، قالب پیام، پیش‌نمایش، و تاریخچهٔ ارسال — زیر هم روی هم
انباشته بود؛ برای رسیدن به تاریخچه باید از وسط کلی گزینه رد می‌شدی.

### الان: سه بخش مجزا و منظم
سوییچ رنگی بالای صفحه: **«ارسال سریع»** (سبز) · **«پیام دستی»** (بنفش) ·
**«تاریخچه»** (آبی) — فقط یکی در هر لحظه نشون داده می‌شه، نه همه با هم.

### مورد غیرضروری حذف‌شده
گزینهٔ مخاطب **«جذب مجدد»** (با اسلایدر «روز بدون رزرو» و اسلایدر تخفیف)
حذف شد — این دقیقاً همون کاری رو می‌کرد که کارت‌های هوشمند «خواب‌رفته» و
«در خطر ریزش» توی بخش «ارسال سریع» از قبل، با محاسبهٔ واقعی RFM (نه فقط
یه عدد روز ساده)، بهتر انجام می‌دن. نگه‌داشتنش یعنی دو راه موازی برای
یک کار — حذفش کرد صفحه رو سبک‌تر بدون از دست رفتن قابلیت.

### یه بهبود کوچیک دیگه
فیلد «درصد تخفیف» (که فقط برای توکن `{{discount}}` لازمه) دیگه همیشه
روی صفحه نیست — فقط وقتی متن پیام واقعاً از این توکن استفاده کنه ظاهر
می‌شه.

با ۲۷ بررسی تعاملی (چیدمان سه‌بخشی، حذف واقعی گزینهٔ تکراری، جریان کامل
ارسال دسته‌جمعی با محتوای واقعی پیامک، رگرسیون کامل همهٔ ۹ تب) + build
واقعی تایید شد.

---

## سگمنت‌بندی مشتریان — ساده‌تر و در اختیار آرایشگر

### هدفمندتر و کمتر: از ۶ دسته به ۴ دسته
قبلاً: قهرمانان، نیازمند توجه، در خطر ریزش، مشتریان جدید و امیدبخش،
خواب‌رفته، پتانسیل وفاداری — شش دسته که بعضی‌هاشون همپوشانی داشتن (مثلاً
«نیازمند توجه» و «در خطر ریزش» هر دو یعنی «مشتری قبلاً پرتردد بوده ولی
دیگه نمیاد» — پیام پیگیری‌شون هم عملاً یکی بود).

الان **۴ دسته**، هرکدوم با یک اقدام مشخص:
- 👑 **مشتریان وفادار** — تازه اومده و پرتردده → قدردانی/معرفی به دوستان
- 🚨 **در خطر ریزش** — قبلاً پرتردد بوده، مدتیه نیومده → بازگرداندن فوری
- 🌱 **مشتریان جدید** — تازه اومده، هنوز کم‌تردد → پرورش رابطه
- 💤 **غیرفعال** — مدت زیادیه نیومده و کم‌تردد بوده → کمپین سبک بازگشت

هر ۴ دسته الان یه کمپین آمادهٔ ارسال (تب پیامک → ارسال سریع) دارن —
قبلاً فقط ۳ تا از ۶ دسته پیام آماده داشتن.

### در اختیار آرایشگر
تابع دیتابیسی `get_customer_rfm_segments()` قبلاً فقط برای مدیر/مالک
(`is_manager()`) محدود بود — الان برای هر عضو فعال تیم (`is_staff()`،
یعنی مالک، مدیر، یا آرایشگر) باز شده. کارت «سگمنت‌بندی مشتریان» توی تب
باشگاه مشتریان دیگه محدود به مدیر نیست.

با ۱۰ تست واحد روی منطق تقسیم‌بندی جدید (پوشش کامل، بدون همپوشانی یا
جای خالی) + بررسی ساختاری دقیق (هر دو ثابت رابط کاربری دقیقاً ۴ کلید
دارن، دسترسی آرایشگر باز شده) + رگرسیون کامل روی همهٔ تب‌های مدیر و
آرایشگر + build واقعی تایید شد.
